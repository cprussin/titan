// Twitter reuses the Open Graph card verbatim — same 1200×630 art, alt text,
// and content type. Re-export the whole surface so there is a single source.
export { alt, contentType, default, size } from "./opengraph-image";
